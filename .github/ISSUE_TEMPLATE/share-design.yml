name: "🎨 شارك تصميمك | Share Your Design"
description: "شارك تصميم SensorPanel أو Rainmeter Skin الخاص بك مع المجتمع"
title: "[Design] "
labels: ["design", "community"]
body:
  - type: markdown
    attributes:
      value: |
        ## 🎨 شكرًا لمشاركة تصميمك! | Thanks for sharing your design!
        املأ النموذج أدناه وأرفق صورة وملف التصميم.
        Fill out the form below and attach a screenshot and the design file.
        
        > ⚠️ **ملاحظة:** GitHub لا يدعم رفع ملفات `.sensorpanel` أو `.rmskin` مباشرة. يجب عليك **ضغط الملف كـ ZIP** أولاً ثم رفعه.
        >
        > ⚠️ **Note:** GitHub does not support uploading `.sensorpanel` or `.rmskin` files directly. You must **compress the file as ZIP** first, then upload it.
        
        > ✅ **سيتم مراجعة تصميمك من قبل الفريق قبل إضافته للصفحة الرئيسية. شكرًا لصبرك!**
        >
        > ✅ **Your design will be reviewed by the team before being featured on the main page. Thanks for your patience!**

  - type: input
    id: design-name
    attributes:
      label: "📝 اسم التصميم | Design Name"
      description: "اختر اسمًا لتصميمك"
      placeholder: "مثال: Islamic Dark Theme"
    validations:
      required: true

  - type: dropdown
    id: tool
    attributes:
      label: "🔧 الأداة المستخدمة | Tool Used"
      options:
        - AIDA64 SensorPanel
        - Rainmeter
        - Other
    validations:
      required: true

  - type: textarea
    id: screenshot
    attributes:
      label: "📸 صورة التصميم | Design Screenshot"
      description: "اسحب وأفلت الصورة هنا، أو الصقها مباشرة | Drag & drop your screenshot here, or paste it directly"
      placeholder: "اسحب الصورة هنا..."
    validations:
      required: true

  - type: textarea
    id: design-file
    attributes:
      label: "📦 ملف التصميم (مضغوط ZIP) | Design File (as ZIP)"
      description: |
        ⚠️ يجب ضغط الملف كـ ZIP أولاً! اضغط بزر الفأرة الأيمن على الملف → Send to → Compressed (zipped) folder ثم اسحب ملف الـ ZIP هنا.

        ⚠️ You must ZIP the file first! Right-click the file → Send to → Compressed (zipped) folder, then drag the ZIP file here.

        - AIDA64: صدّر من SensorPanel → Export → اضغطه ZIP → ارفعه هنا
        - Rainmeter: صدّر الـ Skin → اضغطه ZIP → ارفعه هنا
      placeholder: "اسحب ملف ZIP هنا..."
    validations:
      required: true

  - type: textarea
    id: description
    attributes:
      label: "📋 وصف التصميم (اختياري) | Description (optional)"
      description: "أخبرنا عن تصميمك — ما الذي يميزه؟ | Tell us about your design — what makes it special?"
      placeholder: "مثال: تصميم داكن بألوان إسلامية مع خط عربي أنيق..."

  - type: input
    id: author
    attributes:
      label: "👤 اسمك أو معرّفك | Your name or handle"
      description: "كيف تريد أن نذكرك في قائمة التصاميم المميزة؟"
      placeholder: "مثال: @username أو اسمك"
